import {describe,it,expect} from 'vitest';import app from '../src/index'
const env={MEMORY_ADMIN_KEY:'key'},headers={'Content-Type':'application/json',Authorization:'Bearer key'}
const call=(path:string,init:RequestInit={})=>app.fetch(new Request('https://test'+path,init),env)
describe('memory CRUD and auth',()=>{it('protects mutations and supports create/read/update/archive',async()=>{expect((await call('/api/memories',{method:'POST'})).status).toBe(401);const created=await call('/api/memories',{method:'POST',headers,body:JSON.stringify({title:'CRUD item',domain:'Project',memoryType:'state',summary:'initial',content:'initial',privacyClass:'PRIVATE'})});expect(created.status).toBe(201);const m:any=await created.json();expect((await call('/api/memories/'+m.id,{headers})).status).toBe(200);const updated=await call('/api/memories/'+m.id,{method:'PUT',headers,body:JSON.stringify({summary:'updated'})});expect((await updated.json() as any).summary).toBe('updated');expect((await call('/api/memories/'+m.id,{method:'DELETE',headers})).status).toBe(200)})})
