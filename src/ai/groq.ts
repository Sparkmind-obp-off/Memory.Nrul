import type { AIMessage, AIProvider } from './provider'
export class GroqProvider implements AIProvider{readonly name='groq';constructor(private apiKey:string,public model='llama-3.3-70b-versatile'){}async chat(messages:AIMessage[]){const r=await fetch('https://api.groq.com/openai/v1/chat/completions',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${this.apiKey}`},body:JSON.stringify({model:this.model,messages,temperature:.2})});if(!r.ok)throw new Error(`AI provider request failed (${r.status})`);const d:any=await r.json();const answer=d?.choices?.[0]?.message?.content;if(typeof answer!=='string'||!answer.trim())throw new Error('AI provider returned an empty response');return answer.trim()}}
